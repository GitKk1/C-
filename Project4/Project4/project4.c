//#define max 2
#include <stdio.h>

int main()
{
	//char arr[max] = { 0,1 };
	//printf("%d", arr[1]);//可以的  define定义的标识符是常量，const限制的是常变量，本质还是变量

	//enum sex{male, female, secret};//枚举常量的使用
	//printf("%d\n", male);
	//printf("%d\n", female);
	//printf("%d\n", secret);  //枚举常量，从0开始
	return 0;
}
